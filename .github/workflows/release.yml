# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Release

on:
  workflow_dispatch:
    inputs:
      # ═════════════════════════════════════════════════════════════════════════
      # VERSION STRATEGY
      # ═════════════════════════════════════════════════════════════════════════
      version-specifier:
        description: "Version bump strategy (auto = conventional commits)"
        required: false
        type: choice
        options:
          - auto
          - major
          - minor
          - patch
          - premajor
          - preminor
          - prepatch
          - prerelease
        default: "auto"

      # ═════════════════════════════════════════════════════════════════════════
      # NPM PUBLISHING OPTIONS
      # ═════════════════════════════════════════════════════════════════════════
      dist-tag:
        description: "NPM distribution tag"
        required: false
        type: choice
        options:
          - latest
          - next
          - beta
          - rc
          - alpha
          - canary
        default: "latest"

      # ═════════════════════════════════════════════════════════════════════════
      # CONTROL FLOW OPTIONS
      # ═════════════════════════════════════════════════════════════════════════
      run-ci:
        description: "Run CI validation before publishing"
        required: false
        type: boolean
        default: true

      dry-run:
        description: "Dry run: preview changes without modifying git or NPM"
        required: false
        type: boolean
        default: false

# Prevent concurrent releases
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

# ═════════════════════════════════════════════════════════════════════════════
# GLOBAL ENVIRONMENT VARIABLES
# ═════════════════════════════════════════════════════════════════════════════
env:
  NODE_VERSION: "20"
  PNPM_VERSION: "10.18.3"

jobs:
  # ═════════════════════════════════════════════════════════════════════════════
  # STEP 1: CI VALIDATION (Optional, skippable)
  # ═════════════════════════════════════════════════════════════════════════════
  validate-ci:
    name: ✅ CI Validation
    runs-on: ubuntu-latest
    if: inputs.run-ci == true
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: none

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          fetch-tags: true # Fetch tags for completeness (though not strictly needed for CI validation)

      - name: ⚙️ Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: 📦 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 🔍 Print Environment Info
        run: pnpm nx report

      - name: 🧪 Test all packages
        run: pnpm nx run-many -t test --all -c ci --parallel=4 --verbose

      - name: 🏗️ Build all packages
        run: pnpm nx run-many -t build --all --parallel=4 --verbose

      - name: ✅ CI validation passed
        run: |
          echo "## ✅ CI Validation Passed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All tests and builds completed successfully." >> $GITHUB_STEP_SUMMARY

  # ═════════════════════════════════════════════════════════════════════════════
  # STEP 2: RELEASE (Version bump, build, changelog, tags, publish)
  # ═════════════════════════════════════════════════════════════════════════════
  release:
    name: 🚀 Release & Publish
    runs-on: ubuntu-latest
    # Only require CI if it was enabled and run
    if: always() && (inputs.run-ci == false || needs.validate-ci.result == 'success' || needs.validate-ci.result == 'skipped')
    needs: [validate-ci]
    timeout-minutes: 15
    permissions:
      contents:
        write # Required to push commits and tags
        # NOTE: Branch protection must allow GitHub Actions to bypass
        # Configure in: Settings → Branches → main → "Allow specified actors to bypass"
        # Add: "GitHub Actions" or this workflow
      id-token: write # ← Required for Trusted Publishing (OIDC)

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          fetch-tags: true
          ref: main # Always checkout main branch for releases (ensures all tags are reachable)
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🔍 Verify we're on main branch
        run: |
          CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
          if [ "$CURRENT_BRANCH" != "main" ]; then
            echo "❌ Error: Release workflow must run on main branch"
            echo "   Current branch: $CURRENT_BRANCH"
            echo "   Expected: main"
            exit 1
          fi
          echo "✅ Confirmed: Running on main branch (all tags are reachable)"

      - name: ⚙️ Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          registry-url: https://registry.npmjs.org/
          cache: "pnpm"

      - name: 📂 Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: 🔄 Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: 📦 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 🔍 Print Environment Info
        run: pnpm nx report

      # ──────────────────────────────────────────────────────────────────────
      # PRE-FLIGHT CHECKS (Before making any changes)
      # ──────────────────────────────────────────────────────────────────────
      - name: 🔍 Pre-Flight Checks
        if: inputs.dry-run == false
        run: |
          echo "🔍 Running pre-flight checks..."

          # Check NPM authentication (Trusted Publishing / OIDC)
          # Note: setup-node@v6 with registry-url automatically configures OIDC authentication
          # NODE_AUTH_TOKEN is set by GitHub Actions when using OIDC
          echo "📦 Checking NPM authentication setup (Trusted Publishing)..."

          # Verify OIDC token is present (set by GitHub Actions when using OIDC)
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "❌ NODE_AUTH_TOKEN not found!"
            echo "   This workflow uses Trusted Publishing (OIDC) for NPM authentication"
            echo "   Ensure:"
            echo "   1. Trusted Publishing is configured on npmjs.com for this repository"
            echo "   2. GitHub Actions has 'id-token: write' permission (already set)"
            echo "   3. setup-node action is configured with registry-url (already set)"
            exit 1
          fi

          echo "✅ OIDC token detected (NODE_AUTH_TOKEN is set)"

          # Verify npm registry is configured correctly
          echo "🔍 Verifying npm registry configuration..."
          NPM_REGISTRY=$(npm config get registry 2>/dev/null || echo "")
          if [ "$NPM_REGISTRY" != "https://registry.npmjs.org/" ]; then
            echo "⚠️  Warning: npm registry is not set to https://registry.npmjs.org/"
            echo "   Current registry: ${NPM_REGISTRY:-not set}"
            echo "   This may cause authentication issues"
          else
            echo "✅ npm registry configured correctly: $NPM_REGISTRY"
          fi

          # Try to verify authentication by checking if we can access one of our packages
          # This is a read-only operation that works with OIDC tokens
          echo "🔍 Verifying package access (read-only check)..."
          TEST_PACKAGE="@forge-js/eslint-plugin-utils"
          if npm view "$TEST_PACKAGE" version --registry=https://registry.npmjs.org/ >/dev/null 2>&1; then
            PACKAGE_VERSION=$(npm view "$TEST_PACKAGE" version --registry=https://registry.npmjs.org/ 2>/dev/null || echo "unknown")
            echo "✅ Successfully accessed package: $TEST_PACKAGE@$PACKAGE_VERSION"
            echo "   This confirms npm registry connectivity"
            echo "   Note: OIDC authentication will be fully validated during publish"
          else
            echo "⚠️  Warning: Could not access package $TEST_PACKAGE"
            echo "   This may be normal if:"
            echo "   - Package doesn't exist yet (first publish)"
            echo "   - Network issues"
            echo "   - Trusted Publishing not fully configured"
            echo "   Authentication will still be validated during actual publish"
          fi

          echo "✅ Pre-flight authentication checks completed"
          echo "   Final authentication will be validated during publish step"

          # Check git state is clean
          echo "📝 Checking git state..."
          if [ -n "$(git status --porcelain)" ]; then
            echo "⚠️  Warning: Working directory has uncommitted changes"
            echo "   This may cause issues during release"
            git status --short
          else
            echo "✅ Git working directory is clean"
          fi

          # Check git tags alignment with package.json versions
          echo "🔍 Verifying git tags alignment with package.json versions..."
          TAG_MISMATCH=false
          for pkg in eslint-plugin eslint-plugin-llm-optimized eslint-plugin-utils cli eslint-plugin-llm eslint-plugin-mcp eslint-plugin-mcp-optimized; do
            if [ -f "packages/$pkg/package.json" ]; then
              PKG_VERSION=$(cat packages/$pkg/package.json | grep '"version"' | cut -d'"' -f4)
              LATEST_TAG=$(git tag | grep "^${pkg}@" | sort -V | tail -1 || echo "")
              if [ -n "$LATEST_TAG" ]; then
                TAG_VERSION=$(echo "$LATEST_TAG" | cut -d'@' -f2)
                if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
                  echo "⚠️  Warning: $pkg version mismatch - package.json=$PKG_VERSION, tag=$TAG_VERSION"
                  TAG_MISMATCH=true
                fi
              else
                echo "ℹ️  No existing tag for $pkg (first release or tag format changed)"
              fi
            fi
          done
          if [ "$TAG_MISMATCH" = "true" ]; then
            echo "⚠️  Version mismatches detected - nx release version will align them"
            echo "   This is expected if versions were manually updated"
          else
            echo "✅ All package versions align with git tags"
          fi

          # Check for potential tag conflicts (tags that would be created)
          echo "🔍 Checking for potential tag conflicts..."
          DRY_RUN_OUTPUT=$(pnpm nx release version --dry-run 2>&1 || true)
          if echo "$DRY_RUN_OUTPUT" | grep -q "No changes detected"; then
            echo "ℹ️  No changes detected for release"
            echo "   This means there are no conventional commits since the last release"
            echo "   The release will skip version bumping"
          else
            echo "✅ Changes detected - release will proceed"
            # Extract potential new versions from dry-run output
            POTENTIAL_TAGS=$(echo "$DRY_RUN_OUTPUT" | grep -oE '[a-z-]+@[0-9]+\.[0-9]+\.[0-9]+' || echo "")
            if [ -n "$POTENTIAL_TAGS" ]; then
              echo "📝 Potential new tags to be created:"
              echo "$POTENTIAL_TAGS" | while read tag; do
                if git rev-parse "$tag" >/dev/null 2>&1; then
                  echo "⚠️  Warning: Tag $tag already exists - this may cause issues"
                else
                  echo "   ✅ $tag (will be created)"
                fi
              done
            fi
          fi

          # Verify we have all necessary tags reachable from main
          echo "🔍 Verifying tag reachability from main branch..."
          MISSING_TAGS=false
          for pkg in eslint-plugin eslint-plugin-llm-optimized eslint-plugin-utils cli eslint-plugin-llm eslint-plugin-mcp eslint-plugin-mcp-optimized; do
            if [ -f "packages/$pkg/package.json" ]; then
              PKG_VERSION=$(cat packages/$pkg/package.json | grep '"version"' | cut -d'"' -f4)
              EXPECTED_TAG="${pkg}@${PKG_VERSION}"
              if git rev-parse "$EXPECTED_TAG" >/dev/null 2>&1; then
                # Check if tag is reachable from current HEAD
                if ! git merge-base --is-ancestor "$EXPECTED_TAG" HEAD 2>/dev/null; then
                  echo "⚠️  Warning: Tag $EXPECTED_TAG exists but is not reachable from main"
                  echo "   This may cause version resolution issues"
                  MISSING_TAGS=true
                fi
              fi
            fi
          done
          if [ "$MISSING_TAGS" = "true" ]; then
            echo "⚠️  Some tags are not reachable - this may affect version resolution"
            echo "   Nx will use fallback version resolution (disk-based) if needed"
          else
            echo "✅ All existing tags are reachable from main branch"
          fi

          echo "✅ All pre-flight checks passed"

      # ──────────────────────────────────────────────────────────────────────
      # PHASE 1: PREPARE VERSION (Build + determine versions + update changelogs)
      # ──────────────────────────────────────────────────────────────────────
      - name: 📝 Prepare Release Version
        id: prepare
        if: inputs.dry-run == false
        run: |
          # Configure git user for automated commits
          git config user.name "Ofri Peretz"
          git config user.email "ofri@snappy.com"

          # Run version command (includes preVersionCommand build for manifest updates)
          if [ "${{ inputs.version-specifier }}" != "auto" ]; then
            echo "📝 Using version strategy: ${{ inputs.version-specifier }}"
            pnpm nx release version ${{ inputs.version-specifier }}
          else
            echo "📝 Using conventional commits to determine version"
            pnpm nx release version
          fi

          echo "status=success" >> $GITHUB_OUTPUT
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ──────────────────────────────────────────────────────────────────────
      # PHASE 2: PUSH VERSION CHANGES TO REPOSITORY
      # ──────────────────────────────────────────────────────────────────────
      # After nx release version, we need to push the commits and tags to main
      # This ensures the repository reflects the new versions that were published
      #
      # IMPORTANT: For this to work with branch protection:
      # 1. Branch protection must allow GitHub Actions to bypass protection
      #    (Settings → Branches → Branch protection → Allow specified actors to bypass)
      # 2. Add "GitHub Actions" or this workflow to the bypass list
      # 3. OR configure branch protection to allow this specific workflow
      #
      # If branch protection cannot be bypassed, the workflow will fail here
      # and you'll need to either:
      # - Configure branch protection to allow this workflow
      # - Or manually merge the version changes via PR
      - name: 📤 Push Version Changes to Repository
        if: inputs.dry-run == false
        run: |
          echo "📤 Pushing version changes to main branch..."
          echo ""
          echo "ℹ️  Note: This workflow requires branch protection to allow GitHub Actions"
          echo "   If push fails, configure branch protection to allow this workflow to bypass"
          echo ""

          # Check if there are commits to push (nx release version creates commits)
          if git diff --quiet HEAD origin/main; then
            echo "ℹ️  No local changes to push (versions already match remote)"
            echo "skip_push=true" >> $GITHUB_ENV
          else
            # Get the commits that were created by nx release version
            COMMITS_TO_PUSH=$(git log origin/main..HEAD --oneline | wc -l)
            TAGS_TO_PUSH=$(git tag --list --no-merged origin/main | wc -l)
            
            echo "📝 Found $COMMITS_TO_PUSH commit(s) and $TAGS_TO_PUSH tag(s) to push"
            echo ""
            
            # Fetch latest from origin to check for conflicts
            git fetch origin main
            
            # Check if main has moved ahead (someone pushed while we were running)
            if ! git merge-base --is-ancestor origin/main HEAD; then
              echo "⚠️  Warning: Main branch has moved ahead"
              echo "   Attempting to rebase local changes on top of origin/main..."
              git rebase origin/main || {
                echo "❌ Rebase failed - conflicts detected"
                echo "   Please resolve conflicts manually and re-run the workflow"
                exit 1
              }
            fi
            
            # Push commits to main (will work if branch protection allows GitHub Actions)
            echo "📤 Pushing commits to main..."
            echo "   Using GITHUB_TOKEN with contents:write permission"
            echo "   This should bypass branch protection if configured correctly"
            echo ""
            
            if git push origin main 2>&1; then
              echo "✅ Successfully pushed commits to main"
              PUSH_SUCCESS=true
            else
              PUSH_ERROR=$?
              echo ""
              echo "❌ Failed to push commits to main (exit code: $PUSH_ERROR)"
              echo ""
              echo "🔧 This usually means branch protection is blocking the push."
              echo "   To fix this, configure branch protection to allow this workflow:"
              echo ""
              echo "   1. Go to: Settings → Branches → Branch protection rules → main"
              echo "   2. Under 'Restrict who can push to matching branches'"
              echo "   3. Add 'GitHub Actions' or this workflow to the bypass list"
              echo "   OR"
              echo "   4. Disable 'Require pull request reviews' for this workflow"
              echo "   OR"
              echo "   5. Add this workflow to 'Allow specified actors to bypass'"
              echo ""
              echo "   Workflow name: Release"
              echo "   Workflow file: .github/workflows/release.yml"
              echo ""
              exit 1
            fi
            
            # Push tags (tags can usually be pushed even with branch protection)
            # Handle tag conflicts gracefully - if tag exists, skip it
            echo ""
            echo "🏷️  Pushing tags..."
            TAG_PUSH_OUTPUT=$(git push origin --tags 2>&1) || TAG_PUSH_EXIT=$?
            if [ "$TAG_PUSH_EXIT" -eq 0 ]; then
              echo "✅ Successfully pushed all tags"
            else
              # Check if error is due to existing tags (this is OK)
              if echo "$TAG_PUSH_OUTPUT" | grep -q "already exists"; then
                echo "ℹ️  Some tags already exist remotely (this is OK)"
                echo "   Existing tags will be skipped"
                # Try to push only new tags
                NEW_TAGS=$(git tag --list --no-merged origin/main 2>/dev/null || echo "")
                if [ -n "$NEW_TAGS" ]; then
                  echo "📝 Pushing only new tags..."
                  echo "$NEW_TAGS" | while read tag; do
                    git push origin "$tag" 2>&1 || echo "   ⚠️  Failed to push $tag (may already exist)"
                  done
                fi
              else
                echo "⚠️  Warning: Tag push failed with unexpected error"
                echo "$TAG_PUSH_OUTPUT"
                echo "   This may be non-critical - tags will be created on next release"
              fi
            fi
            
            echo ""
            echo "✅ Successfully pushed all version changes to main"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ──────────────────────────────────────────────────────────────────────
      # PHASE 3: PUBLISH (preReleaseCommand rebuilds with new versions + publish)
      # ──────────────────────────────────────────────────────────────────────
      - name: 🚀 Publish Packages
        id: publish
        if: inputs.dry-run == false
        run: |
          # Configure git user for automated commits
          git config user.name "Ofri Peretz"
          git config user.email "ofri@snappy.com"

          echo "📝 Publishing with dist tag: ${{ inputs.dist-tag }}"
          echo ""

          # Pre-check: Verify versions to be published don't already exist on npm
          echo "🔍 Pre-checking npm registry for version conflicts..."
          PUBLISH_FAILED=false
          for pkg in "@forge-js/eslint-plugin" "@forge-js/eslint-plugin-llm-optimized" "@forge-js/eslint-plugin-utils" "@forge-js/cli" "eslint-plugin-llm" "eslint-plugin-mcp" "eslint-plugin-mcp-optimized"; do
            # Get package name from path
            if [[ "$pkg" == @* ]]; then
              PKG_NAME="$pkg"
            else
              PKG_NAME="$pkg"
            fi
            
            # Get version from dist package.json
            PKG_DIR=$(echo "$pkg" | sed 's/@forge-js\///' | sed 's/eslint-plugin/eslint-plugin/')
            if [ -f "dist/packages/$PKG_DIR/package.json" ]; then
              VERSION=$(cat dist/packages/$PKG_DIR/package.json | grep '"version"' | cut -d'"' -f4)
              if npm view "$PKG_NAME@$VERSION" version >/dev/null 2>&1; then
                echo "⚠️  Warning: $PKG_NAME@$VERSION already exists on npm"
                echo "   This package will be skipped during publish"
                PUBLISH_FAILED=true
              else
                echo "✅ $PKG_NAME@$VERSION is ready to publish"
              fi
            fi
          done
          if [ "$PUBLISH_FAILED" = "true" ]; then
            echo ""
            echo "⚠️  Some versions already exist on npm"
            echo "   Nx will skip publishing these packages automatically"
            echo "   This is expected if you're re-running a release"
          fi
          echo ""

          # Note: Trusted Publishing (OIDC) works for scoped packages (@forge-js/*)
          # For unscoped packages (eslint-plugin-llm-optimized, eslint-plugin-mcp, etc.),
          # Trusted Publishing requires npm support approval OR use NPM_TOKEN fallback
          # 
          # If you see 404 errors for unscoped packages, you need to either:
          # 1. Contact npm support to enable Trusted Publishing for unscoped packages
          # 2. Use NPM_TOKEN secret for unscoped packages (fallback)
          # 3. Move packages to @forge-js scope
          #
          # The actual publish command will validate authentication
          # If auth fails, publish will fail with a clear error message

          # preReleaseCommand rebuilds packages with updated versions, then publishes
          # This will validate NPM authentication during the publish operation
          pnpm nx release publish --tag ${{ inputs.dist-tag }}
        env:
          NPM_CONFIG_PROVENANCE: true
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Uncomment below if you need NPM_TOKEN for unscoped packages:
          # NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      # ──────────────────────────────────────────────────────────────────────
      # DRY RUN: Just preview changes
      # ──────────────────────────────────────────────────────────────────────
      - name: 🔍 Dry Run Preview
        if: inputs.dry-run == true
        run: |
          echo "🔍 DRY RUN MODE: Previewing changes only"

          # Construct the dry-run flag
          DRY_RUN_FLAG="--dry-run"

          # Preview version changes
          if [ "${{ inputs.version-specifier }}" != "auto" ]; then
            echo "📝 Previewing version strategy: ${{ inputs.version-specifier }}"
            pnpm nx release version ${{ inputs.version-specifier }} $DRY_RUN_FLAG
          else
            echo "📝 Previewing conventional commits version"
            pnpm nx release version $DRY_RUN_FLAG
          fi

      # ──────────────────────────────────────────────────────────────────────
      # SUMMARY REPORTING
      # ──────────────────────────────────────────────────────────────────────
      - name: 📊 Generate Release Summary
        run: |
          if [ "${{ inputs.dry-run }}" = "true" ]; then
            echo "## 🔍 Dry Run Preview Complete" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Version Strategy:** ${{ inputs.version-specifier }}" >> $GITHUB_STEP_SUMMARY
            echo "**Dist Tag:** ${{ inputs.dist-tag }}" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "✅ This was a dry-run preview - no changes committed" >> $GITHUB_STEP_SUMMARY
          else
            echo "## 🎉 Release Complete" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Version Strategy:** ${{ inputs.version-specifier }}" >> $GITHUB_STEP_SUMMARY
            echo "**Dist Tag:** \`${{ inputs.dist-tag }}\`" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Published Packages" >> $GITHUB_STEP_SUMMARY
            echo "- \`@forge-js/eslint-plugin\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`@forge-js/eslint-plugin-llm-optimized\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`@forge-js/eslint-plugin-utils\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`@forge-js/cli\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`@forge-js/eslint-plugin-llm\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`@forge-js/eslint-plugin-mcp\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`@forge-js/eslint-plugin-mcp-optimized\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "To install the new version:" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "pnpm add @forge-js/eslint-plugin@${{ inputs.dist-tag }}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          fi
