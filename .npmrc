# Local npm configuration for forge-js
# Safe to commit - contains no secrets

# Use public npm registry for @forge-js scope
@forge-js:registry=https://registry.npmjs.org/

# Ensure we use public registry by default
registry=https://registry.npmjs.org/

# Authentication tokens are inherited from ~/.npmrc
