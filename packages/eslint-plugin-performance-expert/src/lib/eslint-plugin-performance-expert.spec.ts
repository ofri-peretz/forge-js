import { eslintPluginPerformanceExpert } from './eslint-plugin-performance-expert';

describe('eslintPluginPerformanceExpert', () => {
  it('should work', () => {
    expect(eslintPluginPerformanceExpert()).toEqual(
      'eslint-plugin-performance-expert',
    );
  });
});
