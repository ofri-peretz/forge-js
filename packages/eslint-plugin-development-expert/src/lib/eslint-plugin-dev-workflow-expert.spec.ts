import { eslintPluginDevWorkflowExpert } from './eslint-plugin-dev-workflow-expert';

describe('eslintPluginDevWorkflowExpert', () => {
  it('should work', () => {
    expect(eslintPluginDevWorkflowExpert()).toEqual(
      'eslint-plugin-dev-workflow-expert',
    );
  });
});
