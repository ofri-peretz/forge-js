import { eslintPluginA11yExpert } from './eslint-plugin-a11y-expert';

describe('eslintPluginA11yExpert', () => {
  it('should work', () => {
    expect(eslintPluginA11yExpert()).toEqual('eslint-plugin-a11y-expert');
  });
});
