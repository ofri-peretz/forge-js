import { eslintPluginArchitectureExpert } from './eslint-plugin-architecture-expert';

describe('eslintPluginArchitectureExpert', () => {
  it('should work', () => {
    expect(eslintPluginArchitectureExpert()).toEqual(
      'eslint-plugin-architecture-expert',
    );
  });
});
