{
  "name": "eslint-plugin-utils",
  // ... existing config ...
  "targets": {
    // ... existing targets ...
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --coverage"
      }
    }
  }
}
